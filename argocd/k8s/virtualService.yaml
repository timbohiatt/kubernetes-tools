---
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: argocd
  namespace: argocd
spec:
  hosts:
  - "argocd.*" # Change it to "*" if you dont have a host name yet
  gateways:
  - istio-system/ingress-gateway
  http:
  - route:
    - destination:
        host: "argocd.*" # Change it to "*" if you dont have a host name yet
        port:
          number: 80